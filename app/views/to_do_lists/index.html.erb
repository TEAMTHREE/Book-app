<h1> Todo List </h1>
<a href="/to_do_lists/new"> <button >  Make a new list </button> </a>

<% if @to_do_lists %>
<ul>
  <% @to_do_lists.each do |list| %>
    <li>
      <%= link_to(list.name, to_do_list_path(list.id)) %>
    </li>
  <% end %>
</ul>
<% end %>
